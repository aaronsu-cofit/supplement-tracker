export const metadata = { title: '🩹 傷口智慧照護 | 足踝中心' };

import WoundsClientLayout from './ClientLayout';

export default function WoundsLayout({ children }) {
    return <WoundsClientLayout>{children}</WoundsClientLayout>;
}
