export const metadata = { title: '🦴 足踝照護 | 足踝中心' };

import BonesClientLayout from './ClientLayout';

export default function BonesLayout({ children }) {
    return <BonesClientLayout>{children}</BonesClientLayout>;
}
