# @vitera/types

Shared TypeScript type definitions for the DTX monorepo.

## Usage

```typescript
import type { AuthSource, JWTPayload, Client, AuthSession } from '@vitera/types'
```

## Exported Types

- **Auth Types**: Authentication and authorization related types
- **Client Types**: Client and session database models
